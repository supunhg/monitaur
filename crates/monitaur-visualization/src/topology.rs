// Topology generation from infrastructure graph

#[derive(Default)]
pub struct TopologyGenerator;

impl TopologyGenerator {
    pub fn new() -> Self {
        Self
    }

    pub fn generate(&self) {
        todo!("generate topology from infra graph")
    }
}
