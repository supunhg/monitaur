[package]
name = "monitaur-security"
version = "0.1.0"
edition = "2024"
description = "Infrastructure security analysis engine"

[dependencies]
monitaur-core = { path = "../monitaur-core" }
tokio = { version = "1", features = ["full"] }
regex = "1"
uuid = { version = "1", features = ["v4"] }
tracing = "0.1"
